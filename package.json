{
  "name": "atrinik-metaserver",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:admin": "python3 -m unittest discover -s scripts -p 'test_*.py'",
    "check": "npm run types:check && npm run typecheck && npm test && npm run test:admin && npm run deploy:dry-run",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir dist",
    "types:check": "wrangler types",
    "typecheck": "tsc --noEmit && tsc -p test/tsconfig.json --noEmit && tsc -p tsconfig.node.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "undici": "^7.29.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.2",
    "@types/node": "^26.1.2",
    "@types/tr46": "5.0.1",
    "tsx": "^4.23.9",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "tr46": "6.0.0"
  }
}
